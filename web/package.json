{
  "name": "blink-web",
  "version": "1.0.0",
  "private": true,
  "description": "Blink Terminal project website",
  "scripts": {
    "serve": "python3 -m http.server 8080",
    "build": "node scripts/build.js",
    "dev": "npm run build",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "html-minifier-terser": "^7.2.0"
  }
}
